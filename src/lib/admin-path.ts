/**
 * Where the dashboard lives.
 *
 * To move the secret door, change this string AND rename the matching folder
 * at `src/app/atelier-contessa` — every link, redirect and the footer ornament
 * read from here. Pick something no one would type by accident; obscurity is
 * the doorknob, the password is the lock.
 */
export const ADMIN_PATH = "/atelier-contessa";
